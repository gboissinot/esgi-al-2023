## Description
- DIP and DI illustration with the repository pattern

## Notes
- A Service object uses a Repository object through an abstraction (DIP)
- The repository interface has several implementations : InMemory, Stub, Oracle
- The chosen implementation is injected (DI) at the wiring step in the Main
